<filter docker.**>
  @type docker_metadata
</filter>

<match docker.**>
  @type loki
  url http://loki:3100

  <label>
    job docker
    container ${record["container_name"]}
    image ${record["image_name"]}
  </label>
</match>
